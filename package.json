{
  "dependencies": {
    "cypress": "^13.17.0",
    "cypress-parallel": "^0.14.0"
  },
  "devDependencies": {
    "cypress-mochawesome-reporter": "^3.8.2",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^4.3.0",
    "mochawesome-report-generator": "^6.2.0"
  },
  "scripts": {
    "cy:run": "cypress run",
    "cy:parallel": "cypress-parallel -s cy:run -t 3 -d 'cypress/e2e/grupo1/*'"
  }
}
